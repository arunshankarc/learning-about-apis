package intwquestions;

public class MainClass{
	public static void main(String[] args)	{
		
		// Variables are compile time while methods are run-time
	    Honda_bikes obj1 = new Honda_bikes();
	    ParentClass obj2 = new Honda_bikes();
	    ParentClass obj3 = new ParentClass();

	    obj1.disp_speed();
	    obj2.disp_speed();
	    obj3.disp_speed();

	    System.out.println("Max_Speed = " + obj1.max_speed);
	    System.out.println("Max_Speed = " + obj2.max_speed);
	    System.out.println("Max_Speed = " + obj3.max_speed);
	}
}
